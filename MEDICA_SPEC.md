# אפיון ושלד ראשוני - מערכת Medica

**לקוח:** SQLINK / בתי החולים רפאל
**מסמך:** אפיון מוצר + הנחיית בנייה לסוכן פיתוח (Claude Code)
**גרסה:** 0.1 - פרוטוטייפ ראשוני
**מטרת המסמך:** להוות מקור אמת יחיד לבניית mockup חי, לחיץ ומעוצב, שיוצג ללקוח כאפיון ותחושה.

---

## 0. הוראות לסוכן הפיתוח

> קרא את הסעיף הזה במלואו לפני שאתה כותב שורת קוד.

### מה בונים

פרוטוטייפ front-end בלבד. אין backend, אין בסיס נתונים, אין אימות אמיתי. כל הנתונים מגיעים מקבצי mock מקומיים. המטרה היא מוקאפ עובד שאפשר ללחוץ בו על הכל, לא מערכת production.

### Stack מחייב

```
React 18 + TypeScript
Vite
Tailwind CSS
React Router v6
lucide-react            (אייקונים)
date-fns + date-fns/locale/he   (תאריכים בעברית)
```

אין להוסיף ספריות UI מוכנות (MUI, Ant, shadcn). כל הרכיבים נבנים ידנית לפי ה-tokens בסעיף 3, כדי שהעיצוב יהיה שלנו ולא של ספרייה.

### כללי ברזל

1. **RTL בכל המערכת.** `<html dir="rtl" lang="he">`. כל layout ב-Tailwind נבנה עם `start`/`end` ולא `left`/`right`.
2. **אין localStorage, sessionStorage או כל אחסון דפדפן.** מצב נשמר ב-React state בלבד.
3. **אין קריאות רשת.** כל הנתונים מיובאים מ-`src/mock/`.
4. **כל מסך נגיש דרך route.** אין מסך שקיים רק כקומפוננטה.
5. **כל מסך חייב שלושה מצבים:** תוכן, ריק, טעינה. מסכים עם פעולות חייבים גם מצב שגיאה.
6. **מזהים בקוד באנגלית, טקסט ממשק בעברית.** כל מחרוזות הממשק מרוכזות ב-`src/i18n/he.ts`.
7. **פונקציונליות מדומה:** לחיצה על "שמור" מציגה Toast הצלחה ומעדכנת state מקומי. לחיצה על "הורד מסמך" פותחת קובץ PDF דמה מ-`public/mock-files/`.
8. `npm install && npm run dev` חייב לעלות נקי, ללא שגיאות קונסול.

### סדר עבודה

בנה לפי הסדר בסעיף 12. אל תתחיל מסך לפני שרכיבי הבסיס שהוא צריך קיימים.

---

## 1. רקע ומטרות

מערכת Medica משרתת כיום את המנתחים בבתי החולים רפאל ואלישע. היא מרכזת את יומן הניתוחים, יצירת ניתוחים חדשים, שיבוץ והחלפת ניתוחים וצירוף מסמכים רפואיים. המערכת פעילה, אך שפת העיצוב שלה מיושנת.

הפרויקט כולל שני חלקים:

**א. מתיחת פנים** למסכי המנתח הקיימים, תוך שמירה על המבנה הפונקציונלי ועל זהות המותג.

**ב. אזור מטופל חדש**, שנבנה מאפס ומאפשר למטופל לראות את התורים שלו ולהוריד מסמכים רפואיים.

### עקרונות עיצוב מנחים

| עיקרון | משמעות מעשית |
|---|---|
| מידע קריטי קודם | במסך המנתח, שעה ושם מטופל הם המידע הראשי. כל השאר משני. |
| פחות רעש | הסרת הרקע המטושטש, צמצום גבולות, שימוש ברווח לבן להפרדה. |
| מצב תמיד ברור | לכל שורה, כרטיס ופעולה יש מצב חזותי חד משמעי. |
| שתי שפות לשני קהלים | ממשק המנתח צפוף ויעיל. ממשק המטופל רגוע, גדול וידידותי. |
| נגישות מובנית | ניגודיות AA, יעדי מגע 44px, מצב focus נראה. |

---

## 2. משתמשים ותפקידים

### 2.1 מנתח (Doctor)

מנהל את יומן הניתוחים שלו ושל מנתחים שבניהולו. מגיע למערכת בין ניתוחים, לרוב במחשב שולחני, לעיתים בטאבלט. רוצה לראות במבט אחד מה יש לו היום ולבצע שינוי מהיר.

**תרחישי ליבה:**

1. כניסה, צפייה ביומן היום, מעבר בין ימים.
2. מעבר בין מנתחים שבניהולו.
3. חיפוש ניתוח בטווח תאריכים וייצוא לאקסל.
4. יצירת ניתוח חדש דרך אשף בן שלושה שלבים.
5. החלפת יום ושעה לניתוח קיים.

### 2.2 מטופל (Patient)

נכנס מדי פעם, לרוב מהנייד, כדי לבדוק מתי התור הבא ולהוריד מסמך. לא מכיר מונחים מערכתיים. צריך מעט מאוד אפשרויות ומידע ברור.

**תרחישי ליבה:**

1. כניסה עם תעודת זהות ואימות ב-SMS.
2. צפייה בתור הקרוב.
3. סינון התורים העתידיים לפי מחלקה ולפי רופא.
4. צפייה בהיסטוריית התורים.
5. הורדת מסמך רפואי, למשל תוצאות MRI.

---

## 3. שפה עיצובית - Design Tokens

מוגדר ב-`tailwind.config.js` תחת `theme.extend` וגם כ-CSS variables ב-`src/styles/tokens.css`.

### 3.1 צבע

```js
// Brand - סגול Medica, מרוענן
primary: {
  50:  '#F7F5FC',
  100: '#EDE8F8',
  200: '#D9CFF0',
  300: '#B9A7E2',
  400: '#8F74CD',
  500: '#6B4BB8',   // interactive default
  600: '#573A9E',
  700: '#4A2C82',   // brand core - Header, כותרות
  800: '#3A2166',
  900: '#2A1749',
}

// Hospital identity - שימוש בצ'יפים בלבד
hospital: {
  refael: '#2B7FC4',
  elisha: '#0F9B8E',
}

// Semantic
success: '#12876F',
warning: '#C97A16',
danger:  '#C93F4E',
info:    '#2B7FC4',

// Neutral
ink:     '#1A1A22',   // כותרות
body:    '#4B4B58',   // טקסט רגיל
muted:   '#82828F',   // תוויות, מטא
line:    '#E5E2EC',   // גבולות
surface: '#FFFFFF',   // כרטיסים
canvas:  '#F6F5FA',   // רקע עמוד
```

**החלטה חשובה:** מוחקים את רקע התמונה המטושטש שקיים היום. הרקע הופך ל-`canvas` אחיד. זה לבדו מרים את כל המערכת.

### 3.2 טיפוגרפיה

גופן: **Assistant** (Google Fonts), משקלים 400 / 600 / 700.

| Token | גודל | Line height | משקל | שימוש |
|---|---|---|---|---|
| `display` | 32px | 1.25 | 700 | כותרת מסך מטופל |
| `h1` | 26px | 1.3 | 700 | כותרת מסך |
| `h2` | 20px | 1.35 | 600 | כותרת אזור |
| `h3` | 17px | 1.4 | 600 | כותרת כרטיס |
| `body` | 15px | 1.55 | 400 | טקסט רגיל |
| `body-strong` | 15px | 1.55 | 600 | הדגשה |
| `caption` | 13px | 1.45 | 400 | תוויות שדה, מטא |
| `mono-num` | 15px | 1.4 | 600 | שעות, ת.ז, קודים - `tabular-nums` |

מספרים בטבלאות ובשעות תמיד `font-variant-numeric: tabular-nums` כדי שהעמודות יתיישרו.

### 3.3 מרווח, פינות, צל

```
spacing scale: 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64

radius:  sm 8px | md 12px | lg 16px | xl 24px | full 9999px

shadow-sm:  0 1px 2px rgba(26,26,34,.06)
shadow-md:  0 4px 16px rgba(26,26,34,.08)
shadow-lg:  0 12px 32px rgba(26,26,34,.12)   // מודלים בלבד

border: 1px solid line
```

### 3.4 תנועה

```
fast   120ms ease-out    // hover, focus
base   200ms ease-out    // הופעת אלמנט, פתיחת תפריט
slow   320ms cubic-bezier(.22,1,.36,1)   // מודל, מגירה
```

אין אנימציות דקורטיביות. תנועה משמשת רק כדי להסביר מהיכן משהו הגיע.

### 3.5 מצבי אינטראקציה

לכל רכיב לחיץ:

| מצב | טיפול |
|---|---|
| default | לפי הרכיב |
| hover | הבהרה או הכהיה של 6%, `transition fast` |
| focus-visible | `outline: 2px solid primary-500; outline-offset: 2px` |
| active | הכהיה של 10%, `scale(.99)` |
| disabled | `opacity .45`, `cursor: not-allowed`, ללא hover |
| loading | ספינר במקום התוכן, רוחב הרכיב נשמר |

---

## 4. ניווט ו-Routes

```
/                          → redirect לפי סוג משתמש
/login                     כניסת מנתח - תעודת זהות
/verify                    אימות OTP - מנתח
/doctor/:doctorId/schedule יומן ניתוחים (מסך הבית של המנתח)
/doctor/:doctorId/all      כל הניתוחים - טבלה וסינון
/surgery/new               אשף יצירת ניתוח
/surgery/:surgeryId/edit   עריכת ניתוח (אותו אשף, טעון בנתונים)
/surgery/:surgeryId        צפייה בניתוח, לקריאה בלבד

/p/login                   כניסת מטופל
/p/verify                  אימות OTP - מטופל
/p                         המסך הראשי למטופל
/p/upcoming                תורים עתידיים
/p/past                    תורים קודמים
/p/appointment/:id         פרטי תור
/p/documents               כל המסמכים הרפואיים
```

מודל החלפת ניתוח אינו route. הוא מודל שנפתח מעל `/doctor/:id/schedule` או `/doctor/:id/all`.

**מסך פיתוח נוסף:** `/kitchen-sink` - עמוד שמציג את כל הרכיבים בכל המצבים. נועד לבדיקה עצמית ולהצגה מהירה של המערכת העיצובית. לא חלק מהמוצר.

---

## 5. מבנה קבצים

```
src/
  main.tsx
  App.tsx                  Router + AppShell
  styles/
    tokens.css
    globals.css
  i18n/
    he.ts                  כל מחרוזות הממשק
  types/
    index.ts               כל ה-types מסעיף 6
  mock/
    doctors.ts
    patients.ts
    surgeries.ts
    appointments.ts
    documents.ts
    departments.ts
    blocks.ts
    lookups.ts             סוגי מסמכים, סוגי הרדמה, קופות חולים, ציוד
  lib/
    date.ts                עיצוב תאריך ושעה בעברית
    format.ts              ת.ז, טלפון, משך
    cn.ts                  מיזוג classNames
  components/
    primitives/            Button, Input, Select, Checkbox, Radio, Toggle, Textarea
    data/                  Table, Card, Chip, Badge, Avatar, EmptyState, Skeleton
    overlay/               Modal, Drawer, Dropdown, Tooltip, Toast
    form/                  Field, DatePicker, TimePicker, FileUpload, Stepper
    calendar/              MonthCalendar, BlockLegend
    layout/                AppShell, TopBar, DoctorSidebar, PatientNav, PageHeader
  features/
    auth/
    doctor-schedule/
    doctor-surgeries/
    surgery-wizard/
    surgery-swap/
    patient-home/
    patient-appointments/
    patient-documents/
  pages/                   קומפוננטת route אחת לכל route מסעיף 4
public/
  mock-files/
    mri-result.pdf
    discharge-letter.pdf
    blood-test.pdf
```

---

## 6. מודל נתונים

```ts
// ---------- משותף ----------
type ID = string;
type ISODate = string;   // '2026-07-29'
type Time = string;      // '16:00'

type Hospital = 'refael' | 'elisha';

// ---------- מנתח ----------
interface Doctor {
  id: ID;
  firstName: string;
  lastName: string;
  displayName: string;       // "ד\"ר דוחנו אולג"
  title: 'ד"ר' | 'פרופ\'' | '';
  avatarUrl?: string;
  email: string;
  mobile: string;
  licenseNumber: string;     // מספר רישיון
  departmentId: ID;
  managedByMe: boolean;      // מופיע בסרגל "מנתחים בניהולי"
}

// ---------- מחלקה ----------
interface Department {
  id: ID;
  name: string;              // "אורתופדיה", "כירורגיה כללית"
}

// ---------- מטופל ----------
interface Patient {
  id: ID;
  firstName: string;
  lastName: string;
  idType: 'id' | 'passport';
  idNumber: string;
  phone: string;
  birthDate: ISODate;
  gender: 'male' | 'female';
  hmo: string;               // קופת חולים
  payer: string;             // גורם מממן
}

// ---------- ניתוח ----------
interface Surgery {
  id: ID;
  code: string;              // "198787"
  hospital: Hospital;
  doctorId: ID;
  patient: Patient;

  procedures: Procedure[];   // ניתוח אחד או יותר
  date: ISODate;
  startTime: Time;
  durationMinutes: number;
  combined: boolean;         // ניתוח משולב
  backupDoctorName?: string; // רופא מגבה

  anesthesia: 'general' | 'regional' | 'local' | 'sedation';
  capitalEquipment?: string;
  additionalEquipment?: string;
  requirements: RequirementKey[];
  treatmentType: string;

  surgeonFee?: { enabled: boolean; amount: number };

  documents: MedicalDocument[];
  status: 'scheduled' | 'done' | 'cancelled';
  summaryUrl?: string;       // סיכום ניתוח
  dischargeLetterUrl?: string; // מכתב שחרור
}

interface Procedure {
  id: ID;
  name: string;              // "מיני מעקף קיבה בלפרוסקופיה"
  organ?: string;            // איבר
  side?: 'right' | 'left' | 'none';
}

type RequirementKey =
  | 'pacemaker'          // המטופל בעל קוצב לב
  | 'bloodTypeCross'     // סוג דם והצלבה
  | 'preOp'              // טרום ניתוח
  | 'icu'                // טיפול נמרץ
  | 'frozenSection'      // חתך קפוא
  | 'nerveMonitoring'    // ניטור עצבי
  | 'xrayTech'           // טכנאי רנטגן
  | 'agent'              // סוכן
  | 'breastFnlMarking'   // סימון FNL בניתוחי שד
  | 'breastIsotope'      // נדרש סימון איזוטופי בניתוחי שד
  | 'surgicalAssistant'; // עוזרי מנתח

// ---------- מסמך ----------
interface MedicalDocument {
  id: ID;
  typeKey: string;           // מפתח מתוך lookups.documentTypes
  typeLabel: string;         // "בדיקות דימות MRI / CT / US"
  fileName: string;
  fileUrl: string;           // '/mock-files/mri-result.pdf'
  sizeKb: number;
  uploadedAt: ISODate;
  source: 'doctor' | 'hospital';
}

// ---------- בלוק ניתוחים (ליומן) ----------
interface Block {
  id: ID;
  hospital: Hospital;
  date: ISODate;
  startTime: Time;
  endTime: Time;
  doctorId: ID;
  state: 'full' | 'partial' | 'open';  // מלא / יש שעות פנויות / פנוי
}

// ---------- תור מטופל ----------
interface Appointment {
  id: ID;
  patientId: ID;
  kind: 'surgery' | 'consult' | 'test' | 'followup';
  title: string;             // "ייעוץ אורתופדי", "MRI ברך ימין"
  departmentId: ID;
  departmentName: string;
  doctorId: ID;
  doctorName: string;
  hospital: Hospital;
  date: ISODate;
  time: Time;
  location: string;          // "בניין A, קומה 2, חדר 214"
  status: 'upcoming' | 'completed' | 'cancelled';
  preparation?: string[];    // הנחיות הכנה
  documents: MedicalDocument[];
  resultSummary?: string;    // תקציר תוצאה, לתורים שהסתיימו
}
```

---

## 7. מפרט מסכים

לכל מסך: מטרה, פריסה, נתונים, אינטראקציות, מצבים.

---

### 7.1 כניסה - מנתח (`/login`)

**מטרה:** הזדהות בתעודת זהות.

**פריסה:** מסך מפוצל. שני שלישים ימין - רקע `primary-700` מלא עם הלוגו במרכז. שליש שמאל - כרטיס לבן על `canvas` עם הטופס. במובייל: הכל בעמודה אחת, הלוגו למעלה.

**תוכן הכרטיס:**

- כותרת `h1`: "כניסה למערכת"
- טקסט `caption` בצבע `muted`: "הזדהות באמצעות תעודת זהות"
- שדה: תעודת זהות, `inputMode="numeric"`, 9 ספרות, מסכת הזנה
- כפתור ראשי מלא רוחב: "המשך"
- קישור `caption`: "כניסה כמטופל" → `/p/login`

**ולידציה:** פחות מ-9 ספרות → "מספר תעודת זהות אינו תקין". שדה ריק בשליחה → "שדה חובה".

**מעבר:** לחיצה על המשך → `/verify`, מדומה, כל ת.ז מתקבלת.

---

### 7.2 אימות OTP (`/verify`, `/p/verify`)

**מטרה:** הזנת קוד חד פעמי.

**פריסה:** זהה ל-7.1.

**תוכן:**

- כותרת: "כניסה עם קוד חד פעמי ב-SMS"
- טקסט: "קוד האימות נשלח כעת לטלפון הנייד שמספרו: xxx-xxx-2667"
- שישה תיבות ספרה נפרדות, מעבר אוטומטי בין תיבות, תמיכה בהדבקה
- טיימר: "אפשר לשלוח קוד חדש בעוד 00:47"
- כשהטיימר מסתיים: קישור פעיל "שליחת קוד חוזר"
- כפתור ראשי: "אישור"

**מצב שגיאה:** קוד שגוי → תיבות באדום `danger` + הודעה "הקוד שהוזן אינו נכון, נסו שוב".

**מדומה:** כל קוד בן שש ספרות מתקבל. הקוד `000000` מפעיל את מצב השגיאה, לצורך הדגמה.

---

### 7.3 יומן ניתוחים (`/doctor/:doctorId/schedule`) - מסך הבית של המנתח

זה המסך החשוב במערכת. הוא מקבל את מרב תשומת הלב העיצובית.

**פריסה - Desktop (≥1280px):** שלושה אזורים.

```
┌──────────────────────────── TopBar ────────────────────────────┐
│ ┌─────── סרגל מנתחים ──────┐ ┌──── תוכן ראשי ────┐             │
│ │ (רוחב 264px, ימין)       │ │ (גמיש)            │             │
│ │                          │ │ ┌── רשימת היום ──┐│ ┌─ צד ─┐   │
│ │                          │ │ │                ││ │ 320px│   │
│ └──────────────────────────┘ └───────────────────┘ └──────┘   │
└────────────────────────────────────────────────────────────────┘
```

בעברית RTL: סרגל המנתחים בקצה **ימין**, רשימת היום במרכז, לוח השנה בקצה **שמאל**.

#### TopBar

- לוגו Medica בקצה ימין
- כותרת המסך במרכז: `יומן ניתוחים` + מפריד דק + `ד"ר דוחנו אולג`
- אווטאר המנתח + תפריט משתמש בקצה שמאל
- שורה שנייה דקה מתחת: אימייל, נייד, מספר רישיון - כטקסט `caption` עם אייקון קטן לכל אחד

**שינוי מהמצב הקיים:** היום פרטי הקשר צפים על הרקע. בעיצוב החדש הם בשורת מטא מובנית עם רקע `primary-800`.

#### סרגל מנתחים (`DoctorSidebar`)

- כותרת: "מנתחים בניהולי"
- שדה חיפוש בראש הרשימה - **תוספת חדשה**, הרשימה היום ארוכה מאוד וללא חיפוש
- רשימה נגללת. כל פריט: אווטאר 36px, שם, מתחת בשורת `caption` מספר הניתוחים היום ("3 ניתוחים היום") - **תוספת חדשה**
- מצב נבחר: רקע `primary-100`, פס `primary-700` ברוחב 3px בקצה ההתחלה
- בתחתית הסרגל, מקובע: כפתור "יציאה" עם אייקון

#### רשימת היום

כרטיס `surface` עם `radius-lg` ו-`shadow-sm`.

**כותרת הכרטיס:** רקע `primary-700`, טקסט לבן.

- חץ אחורה, תאריך `29/07/26`, חץ קדימה
- קישור בקצה השני: "שינוי הל\"ז"
- מתחת לתאריך, ב-`caption`: "יום רביעי" + "4 ניתוחים, 4:20 שעות" - **תוספת חדשה**

**שורת ניתוח:** גובה 72px, `border-bottom: 1px solid line`.

מימין לשמאל:

1. תפריט פעולות (שלוש נקודות אנכיות)
2. תיאור הניתוח - `body` בצבע `body`, חיתוך עם `...` ו-tooltip מלא
3. שם המטופל `body-strong` + מתחת ת.ז ב-`caption` `muted`
4. צ'יפ בית חולים: "רפאל" ברקע `hospital.refael`, "אלישע" ברקע `hospital.elisha`, טקסט לבן, `radius-full`
5. טווח שעות `mono-num` + אייקון שעון

**hover על שורה:** רקע `primary-50`, פס `primary-500` ברוחב 3px בקצה ההתחלה.

**שורת חלון פנוי:** רקע `success` בשקיפות 8%, פס `success` בקצה, טקסט "פנוי" + טווח השעות. לחיצה פותחת את אשף יצירת הניתוח עם התאריך והשעה כבר ממולאים.

**תפריט פעולות** (`Dropdown`), ארבעה פריטים עם אייקונים:

| פריט | אייקון | פעולה |
|---|---|---|
| עריכה | pencil | `/surgery/:id/edit` |
| החלפה | arrow-left-right | פותח מודל 7.5 |
| מחיקה | trash | מודל אישור, ואז Toast |
| לצפייה | monitor | `/surgery/:id` |

**באנר תחתון:** רקע `primary-50`, גבול `primary-200`, אייקון מידע. "שים לב, קיים לך במערכת ניתוח משולב עם ד\"ר בורג אלון ליצירת הניתוח לחץ כאן". המילים "לחץ כאן" הן קישור.

#### לוח שנה חודשי (`MonthCalendar`)

- כותרת: חץ, "יולי 2026", חץ
- שורת ימים: א ב ג ד ה ו ש
- תא יום: 40px, `radius-md`
- **היום** - עיגול `primary-700` מלא, טקסט לבן
- **יום נבחר** - טבעת `primary-500` ברוחב 2px
- **יום עם ניתוחים** - נקודה קטנה מתחת למספר, בצבע בית החולים
- **תג עומס** - צ'יפ קטן מתחת ליום עם מספר שעות מתוכננות, למשל `2.66`, רקע `primary-200`

מתחת ללוח - **מקרא בלוקים** (`BlockLegend`), שתי שורות:

```
רפאל    ● בלוק מלא   ○ בלוק   ◐ שעות פנויות בבלוק
אלישע   ● בלוק מלא   ○ בלוק   ◐ שעות פנויות בבלוק
```

**שיפור:** היום המקרא מבלבל. בעיצוב החדש כל בית חולים בשורה משלו, עם שם הבית חולים ב-`body-strong` ובצבע הזהות שלו, והסמלים בגודל אחיד.

**מצבים:**

- טעינה: שלוש שורות Skeleton ברשימה, לוח שנה בגווני אפור
- ריק: איור קל, "אין ניתוחים ליום זה", כפתור משני "יצירת ניתוח"

**רספונסיביות:**

- `<1280px` - לוח השנה עובר למגירה שנפתחת מכפתור "לוח שנה" בראש הרשימה
- `<1024px` - סרגל המנתחים הופך לתפריט נפתח בראש המסך
- `<768px` - שורות הניתוח הופכות לכרטיסים בעמודה אחת

---

### 7.4 כל הניתוחים (`/doctor/:doctorId/all`)

**מטרה:** חיפוש ניתוחים בטווח תאריכים וייצוא.

**פריסה:** TopBar + סרגל מנתחים כמו 7.3. אזור התוכן: כותרת עמוד, שורת סינון, טבלה.

**כותרת:** "כל הניתוחים" + קישור חזרה "ליומן הניתוחים" עם חץ.

**שורת סינון** (כרטיס `surface`, `radius-md`, ריפוד 16px):

| שדה | סוג |
|---|---|
| שם ניתוח | Select עם חיפוש, מרובה |
| מתאריך | DatePicker |
| עד תאריך | DatePicker |
| בית חולים | Select - **תוספת חדשה** |
| נקה סינון | קישור טקסט - **תוספת חדשה** |

הסינון מוחל אוטומטית בשינוי, ללא כפתור "חפש". מעל הטבלה מוצג `caption`: "נמצאו 4 ניתוחים".

**טבלה** - עמודות מימין לשמאל:

1. בית חולים (צ'יפ)
2. קוד הניתוח (`mono-num`)
3. שם הניתוח
4. שם מטופל
5. ת.ז / דרכון (`mono-num`)
6. תאריך ניתוח (`mono-num`)
7. שעת ניתוח (`mono-num`)
8. משך ניתוח (`mono-num`, בדקות)
9. סיכום ניתוח (אייקון הורדה, או `–` אם אין)
10. מכתב שחרור (אייקון הורדה, או `–` אם אין)
11. תפריט פעולות

**עיצוב הטבלה:**

- כותרת: רקע `primary-700`, טקסט לבן, `caption` במשקל 600, דביקה בגלילה
- שורות: גובה 56px, ללא פסים מתחלפים, מפריד `line` בלבד
- hover: רקע `primary-50`
- מיון בלחיצה על כותרת עמודה, חץ קטן מציין את הכיוון

**תחתית:** קישור "ייצוא לקובץ XL" עם אייקון קובץ. במוקאפ - מוריד CSV מדומה.

**מצב ריק:** במקום `No results found` באנגלית. איור קל + "לא נמצאו ניתוחים בטווח התאריכים שנבחר" + כפתור משני "נקה סינון".

**רספונסיביות:** מתחת ל-1024px הטבלה הופכת לרשימת כרטיסים. כל כרטיס מציג את המידע הראשי, והשאר נפתח באקורדיון.

---

### 7.5 מודל החלפת ניתוח

**מטרה:** בחירת יום ושעה חלופיים לניתוח קיים.

**נפתח מ:** תפריט פעולות במסכים 7.3 ו-7.4.

**מבנה:** `Modal` ברוחב 720px, `radius-xl`, `shadow-lg`, רקע כהה חצי שקוף מאחור.

**כותרת:** "החלפת ניתוח" + כפתור X.

**שורת סיכום** - רצועה ברקע `primary-50`, `radius-md`, פריטים מופרדים בקו אנכי דק, כל אחד עם אייקון:

```
קוד 2000  |  עידית גאז  |  29/07/2026  |  16:00  |  ת.ז 37336278
```

**גוף המודל - שתי עמודות:**

- ימין: "בחר יום החלפה" → `MonthCalendar`
- שמאל: "בחר שעת החלפה" → רשימת חלונות זמן פנויים כרשת כפתורים

**שיפור מהותי:** היום השעה מוצגת כשדה בודד. בעיצוב החדש - רשת של חלונות פנויים לפי הבלוקים של אותו יום, עם הצגת מצב הבלוק בצבע. זה הופך את המודל לשימושי במקום למבלבל.

מתחת - `BlockLegend` זהה ל-7.3.

**תחתית:** כפתור ראשי "אישור החלפה" (מושבת עד שנבחרו יום ושעה) + כפתור טקסט "ביטול".

**לאחר אישור:** המודל נסגר, Toast הצלחה "הניתוח הועבר ל-22/07/2026 בשעה 10:00", השורה ביומן מתעדכנת ומהבהבת פעם אחת ב-`success` בשקיפות נמוכה.

---

### 7.6 אשף יצירת ניתוח (`/surgery/new`)

שלושה שלבים. הכותרת: "כל הניתוחים | שם המנתח". מתחת - `Stepper` אופקי.

**עיצוב ה-Stepper:** שלושה עיגולים מחוברים בקו. שלב הושלם - עיגול `primary-700` מלא עם וי. שלב נוכחי - עיגול `primary-700` מלא עם המספר וטבעת `primary-200` מסביב. שלב עתידי - עיגול לבן עם גבול `line` ומספר ב-`muted`. תחת כל עיגול - שם השלב. שלב שהושלם ניתן ללחיצה.

הטופס נמצא בכרטיס `surface` ברוחב מרבי 720px, ממורכז.

---

#### שלב 1 - פרטי המטופל

| שדה | סוג | הערות |
|---|---|---|
| שם פרטי | Input + autocomplete | חיפוש במאגר מטופלים, dropdown תוצאות |
| שם משפחה | Input | |
| סוג מזהה | Radio: ת.ז / דרכון | |
| מספר מזהה | Input numeric | ולידציית ת.ז |
| טלפון | Input tel | |
| תאריך לידה | DatePicker | |
| מין | Radio: זכר / נקבה | |
| קופת חולים | Select | מ-`lookups.hmos` |
| גורם מממן | Select | מ-`lookups.payers` |
| שכר מנתח | Toggle + Input סכום | הערה: "*לגבייה מהמנותחת בלבד" |

**תחתית:** כפתור ראשי "המשך". מתחת לכרטיס, קישור עם חץ: "ליומן הניתוחים".

**Autocomplete:** בהקלדת שתי אותיות ומעלה נפתח dropdown עם עד 5 מטופלים תואמים. בחירה ממלאת אוטומטית את כל שדות המטופל ומציגה `Chip` קטן "מטופל קיים" עם אפשרות ניקוי.

---

#### שלב 2 - פרטי הניתוח

מחולק לשלושה אזורים עם כותרת `h3` וקו מפריד:

**אזור א - הניתוח**

| שדה | סוג |
|---|---|
| שם ניתוח | Select עם חיפוש, שדה חובה |
| איבר | Select |
| צד | Select: ימין / שמאל / ללא צד |
| תאריך | DatePicker |
| שעה | TimePicker |

כפתור "+ הוסף ניתוח" מוסיף שורת ניתוח נוספת, עם כפתור X להסרה. הכפתור מסוגנן כ-secondary עם רקע `primary-700`, כפי שקיים היום.

**אזור ב - ביצוע**

| שדה | סוג | הערות |
|---|---|---|
| משך (בדקות) | Input numeric עם חצים | שדה חובה, ערך > 0 |
| ניתוח משולב | Toggle | |
| שם רופא מגבה | Input עם autocomplete | |

**אזור ג - הרדמה, ציוד ודרישות**

| שדה | סוג |
|---|---|
| הרדמה | Radio: כללית / איזורית / מקומית / סדציה - שדה חובה |
| ציוד קפיטלי | Select |
| ציוד נוסף | Textarea |
| סוג טיפול | Radio |

**דרישות נוספות** - רשת Checkbox בשלוש עמודות:

```
המטופל בעל קוצב לב      חתך קפוא (fs)          ניטור עצבי
סוג דם והצלבה           טכנאי רנטגן            סימון FNL בניתוחי שד
טרום ניתוח              סוכן                   נדרש סימון איזוטופי בניתוחי שד
טיפול נמרץ              עוזרי מנתח             הכנת אורח לזהוד ניתוח
```

**ולידציה:** שדה חובה ריק → גבול `danger` + טקסט `caption` באדום מתחת: "שדה חובה". ערך לא חוקי → "ערך אינו חוקי". שם השדה עצמו הופך לאדום. בניסיון מעבר שלב עם שגיאות - גלילה אוטומטית לשגיאה הראשונה + Toast: "יש להשלים את השדות המסומנים".

**תחתית:** "המשך" (ראשי) + "חזור" (טקסט עם חץ).

---

#### שלב 3 - צירוף מסמכים

**אזור עליון - חובה:**

- תווית `body-strong`: "סיכום אנמנזה רפואית"
- `FileUpload` בסגנון אזור גרירה: מסגרת מקווקוות `primary-300`, אייקון העלאה, "גררו קובץ לכאן או לחצו לבחירה"
- אם חסר: "חובה להעלות קובץ אנמנזה רפואית" ב-`danger`

**אזור מסמכים נוספים:** שורות חוזרות, כל שורה:

- Select "סוג המסמך" מתוך `lookups.documentTypes`
- `FileUpload`
- כפתור X להסרת השורה

כפתור "+ הוספת מסמך" מוסיף שורה.

**רשימת סוגי מסמכים** (`lookups.documentTypes`) - שמור על הרשימה המלאה:

```
רפואי · תעודת זהות · בדיקות דם · משטח אף MRSA · אישור בדיקת קורונה ·
היסטוריה רפואית מרופא משפחה · אישור מרופא מומחה · תרבית שתן ·
הפניה לניתוח · צילום חזה · אישור מועדה בריאטרית · טופס הסכמה מדעת ·
א.ק.ג · תו ירוק · בדיקות דימות MRI / CT / US ·
ייעוץ רופא מומחה (קרדיולוג / רופא ריאות וכו') · רשימת תרופות ·
שאלון רפואי · דוחות רפואיים · אנמנזה רפואית · בקשה לתביעה ·
צו אפוטרופוס · אקו לב / הולטר
```

**הערת מגבלות** ב-`caption` `muted`: "האתר תומך במסמכים מסוג WORD, TIFF, PDF ותמונות, בנפח של עד 4.5 מגה".

**קובץ שהועלה** מוצג ככרטיס קטן: אייקון לפי סוג, שם הקובץ, גודל, כפתור הסרה.

**תחתית:** כפתור ראשי "סיום" + "חזור".

**לאחר סיום:** Toast הצלחה "הניתוח נוצר בהצלחה" + ניווט ל-`/doctor/:id/schedule` עם השורה החדשה מודגשת.

---

### 7.7 כניסת מטופל (`/p/login`)

זהה במבנה ל-7.1, אבל בשפה רגועה יותר.

- כותרת `display`: "האזור האישי שלי"
- טקסט: "כאן תוכלו לראות את התורים שלכם ולהוריד מסמכים רפואיים"
- שדה: תעודת זהות
- כפתור: "כניסה"

**הבדל עיצובי:** גדלי טקסט גדולים יותר בשתי דרגות, שדות בגובה 52px במקום 44px, יותר רווח לבן. הקהל כאן רחב וכולל מבוגרים.

---

### 7.8 המסך הראשי למטופל (`/p`)

**מטרה:** לתת תשובה מיידית לשאלה "מתי התור הבא שלי", ולאפשר גישה לכל השאר.

**פריסה - Desktop:** עמודה מרכזית ברוחב מרבי 880px, ממורכזת. אין סרגל צד. ניווט עליון פשוט.

**PatientNav:** לוגו, שם המטופל, כפתור יציאה. מתחת - שלוש לשוניות: `תורים עתידיים` · `תורים קודמים` · `מסמכים`.

**מבנה העמוד:**

**1. ברכה**

```
שלום עידית
```

`display`, ומתחת ב-`body` `muted`: "יש לך 2 תורים עתידיים".

**2. כרטיס התור הקרוב** - הרכיב הבולט במסך.

- רקע gradient עדין מ-`primary-700` ל-`primary-600`, טקסט לבן, `radius-xl`, `shadow-md`
- תווית קטנה למעלה: "התור הקרוב"
- כותרת `h2`: שם התור, למשל "ייעוץ אורתופדי"
- שורת מטא בשלושה פריטים עם אייקונים: תאריך מלא בעברית ("יום רביעי, 29 ביולי 2026"), שעה, שם הרופא
- שורה שנייה: מחלקה, מיקום
- צ'יפ בפינה: "בעוד 3 ימים"
- כפתור לבן: "לפרטי התור"

**3. תורים עתידיים** - כותרת `h2` + קישור "הצג הכל" ← `/p/upcoming`. מתחת: עד שלושה כרטיסי תור.

**4. תורים קודמים** - כותרת `h2` + קישור "הצג הכל" ← `/p/past`. מתחת: עד שלושה כרטיסים.

**5. מסמכים אחרונים** - כותרת `h2` + קישור "הצג הכל". מתחת: עד שלושה שורות מסמך עם כפתור הורדה.

**מצב ריק:** "אין לך תורים עתידיים כרגע" + טקסט משני.

**רספונסיביות:** במובייל הכל בעמודה אחת, ריפוד צד 16px, הלשוניות הופכות לניווט תחתון מקובע עם אייקונים.

---

### 7.9 תורים עתידיים (`/p/upcoming`)

**מטרה:** רשימת כל התורים העתידיים, עם סינון.

**כותרת:** `h1` "תורים עתידיים" + `caption`: "5 תורים".

**סינון** - שורת `Chip` נגללת אופקית, לא Select. במובייל זה הרבה יותר נוח:

```
[ הכל ]  [ מחלקה ▾ ]  [ רופא ▾ ]
```

לחיצה על "מחלקה" פותחת `Dropdown` עם רשימת המחלקות + checkbox לכל אחת. לחיצה על "רופא" - אותו דבר עם רשימת הרופאים. צ'יפ פעיל: רקע `primary-700`, טקסט לבן, מספר הבחירות בסוגריים ו-X לניקוי.

מעל הרשימה, כשיש סינון פעיל: "מציג 2 מתוך 5 תורים" + קישור "נקה סינון".

**כרטיס תור** (`AppointmentCard`) - `surface`, `radius-lg`, `shadow-sm`, ריפוד 20px:

```
┌─────────────────────────────────────────────┐
│  [ 29 ]   ייעוץ אורתופדי         [ צ'יפ ]  │
│  [ יולי]   ד"ר בורג אלון · אורתופדיה        │
│           🕐 14:30  📍 בניין A, קומה 2      │
│           ─────────────────────────────      │
│           לפרטי התור ←                       │
└─────────────────────────────────────────────┘
```

- בקצה ההתחלה: "בלוק תאריך" - ריבוע `primary-100` עם היום במספר גדול והחודש מתחת
- צ'יפ סוג התור: ניתוח / ייעוץ / בדיקה / מעקב, כל אחד בצבע משלו
- לחיצה על הכרטיס כולו → `/p/appointment/:id`

**מיון:** מהקרוב לרחוק. מפריד תאריך בין קבוצות: "השבוע", "החודש", "בהמשך".

**מצב ריק:** "אין תורים עתידיים התואמים לסינון" + כפתור "נקה סינון".

---

### 7.10 תורים קודמים (`/p/past`)

זהה במבנה ל-7.9, בהבדלים הבאים:

- כותרת: "תורים קודמים"
- מיון: מהחדש לישן. מפרידים: "החודש", "שלושת החודשים האחרונים", "מוקדם יותר"
- כרטיס התור בגוונים מאופקים: בלוק התאריך ב-`canvas` במקום `primary-100`, טקסט ב-`body`
- **תוספת בכרטיס:** אם יש מסמכים מצורפים, מוצגת שורה תחתונה עם אייקון קובץ ומספר: "2 מסמכים" + כפתור טקסט "הורדה"
- אם קיים `resultSummary`, מוצגת שורת תקציר בת שורה אחת, חתוכה, ב-`caption`
- הסינון זהה: מחלקה ורופא

---

### 7.11 פרטי תור (`/p/appointment/:id`)

**מטרה:** כל המידע על תור בודד + הורדת המסמכים שלו.

**מבנה:**

**1. קישור חזרה** - "חזרה לתורים" עם חץ.

**2. כותרת** - `display` שם התור + צ'יפ סוג + צ'יפ סטטוס.

**3. כרטיס פרטים** - רשימת שורות `label / value`, כל שורה עם אייקון:

| תווית | ערך |
|---|---|
| תאריך | יום רביעי, 29 ביולי 2026 |
| שעה | 14:30 |
| רופא | ד"ר בורג אלון |
| מחלקה | אורתופדיה |
| בית חולים | רפאל (צ'יפ) |
| מיקום | בניין A, קומה 2, חדר 214 |

**4. הנחיות הכנה** - מוצג רק אם קיים `preparation`. כרטיס ברקע `primary-50` עם אייקון מידע ורשימת נקודות.

**5. מסמכים רפואיים** - כותרת `h2`. רשימת שורות, כל שורה:

- אייקון סוג קובץ (PDF / תמונה / Word) בריבוע `radius-md`
- שם המסמך `body-strong`, מתחת ב-`caption`: סוג המסמך · תאריך העלאה · גודל
- כפתור בקצה: "הורדה" עם אייקון

לחיצה מורידה קובץ מ-`public/mock-files/`. מסמך MRI מוגדר במפורש בנתוני הדמה: `"תוצאות MRI ברך ימין"` ← `/mock-files/mri-result.pdf`.

**מצב ריק למסמכים:** "טרם צורפו מסמכים לתור זה".

**6. תוצאה** - לתורים שהסתיימו, אם קיים `resultSummary`: כרטיס עם כותרת "תקציר תוצאה" וטקסט.

---

### 7.12 מסמכים רפואיים (`/p/documents`)

**מטרה:** תצוגה מרוכזת של כל המסמכים של המטופל, ללא תלות בתור.

**סינון:** צ'יפים לפי סוג מסמך + שדה חיפוש חופשי.

**תצוגה:** אותה שורת מסמך כמו ב-7.11, מקובצת לפי חודש.

**מצב ריק:** "אין עדיין מסמכים רפואיים באזור האישי שלך".

---

## 8. רשימת רכיבים

בנה בסדר הזה. כל רכיב מקבל ערך ב-`/kitchen-sink`.

### primitives

| רכיב | Props עיקריים | מצבים |
|---|---|---|
| `Button` | `variant: primary/secondary/ghost/danger`, `size: sm/md/lg`, `icon`, `loading`, `fullWidth` | default, hover, focus, active, disabled, loading |
| `Input` | `label`, `error`, `hint`, `icon`, `type`, `dir` | default, focus, filled, error, disabled |
| `Select` | `options`, `searchable`, `multiple`, `placeholder` | + open |
| `Checkbox` / `Radio` | `label`, `checked`, `disabled` | + error |
| `Toggle` | `checked`, `label` | |
| `Textarea` | `rows`, `maxLength` | |

### data

| רכיב | הערות |
|---|---|
| `Table` | כותרת דביקה, מיון, hover, גרסת כרטיסים למובייל |
| `Card` | עטיפה בסיסית עם ריפוד ו-radius |
| `Chip` | `variant: hospital/type/filter`, אפשרות `onRemove` |
| `Badge` | ספירה קטנה |
| `Avatar` | תמונה או ראשי תיבות, גדלים sm/md/lg |
| `EmptyState` | `illustration`, `title`, `description`, `action` |
| `Skeleton` | `line` / `block` / `circle` |

### overlay

`Modal` (sm/md/lg, סגירה ב-Escape ובלחיצה מחוץ, מלכודת focus) · `Drawer` (נפתח מהקצה, למובייל) · `Dropdown` · `Tooltip` · `Toast` (success/error/info, נעלם אחרי 4 שניות, נערם בפינה התחתונה)

### form

`Field` (עוטף label, שדה, hint, error) · `DatePicker` (לוח שנה עברי, ניווט חודשים) · `TimePicker` · `FileUpload` (גרירה, תצוגת קובץ, מגבלת גודל) · `Stepper`

### calendar

`MonthCalendar` (`selectedDate`, `markedDates`, `loadBadges`, `onSelect`) · `BlockLegend`

### layout

`AppShell` · `TopBar` · `DoctorSidebar` · `PatientNav` · `PageHeader`

---

## 9. מצבים גלובליים

| מצב | טיפול |
|---|---|
| טעינת מסך | Skeleton במבנה התוכן האמיתי, לא ספינר במרכז |
| טעינת פעולה | ספינר בתוך הכפתור, הכפתור מושבת |
| רשימה ריקה | `EmptyState` עם פעולה מוצעת |
| שגיאת פעולה | `Toast` אדום + הודעה ליד השדה כשרלוונטי |
| אין הרשאה | מסך ייעודי עם הסבר וכפתור חזרה |
| 404 | מסך ייעודי, "הדף לא נמצא" |

---

## 10. נתוני דמה

צור נתונים ריאליים בעברית. מינימום:

- **8 מנתחים**, מהם 5 עם תמונה. שמות מהמערכת: ד"ר דוחנו אולג, פרופ' דרקסלר מיכאל, פרופ' טיינין, ד"ר בניקם שלווה, פרופ' חזן דוד, ד"ר תבורי אלי, ד"ר בורג אלון, ד"ר פריאל אילת
- **6 מחלקות**: כירורגיה כללית, אורתופדיה, כירורגיה בריאטרית, כירורגיית שד, אורולוגיה, כירורגיית כלי דם
- **20 ניתוחים** על פני 3 שבועות, מפוזרים בין רפאל לאלישע. השתמש בתיאורים אמיתיים מהמערכת: "מיני מעקף קיבה בלפרוסקופיה", "תיקון בקע מפשעתי דו צידי בלפרוסקופיה", "תיקון בקע ונטרלי בגישה לפרוסקופית עם שתל", "תיקון בקע טבורי בלפרוסקופיה", "Achilles tendon repair"
- **יום 29/07/2026** חייב להכיל בדיוק את ארבעת הניתוחים מהמערכת הקיימת (16:00, 17:15, 18:20, 19:20) ואת החלון הפנוי 20:20-23:00, כדי שההשוואה מול המצב הקיים תהיה ישירה
- **1 מטופל מחובר** לאזור המטופל: עידית גאז
- **5 תורים עתידיים** ו-**8 תורים קודמים** למטופל, מפוזרים על פני 4 מחלקות ו-4 רופאים - חובה, אחרת הסינון לא ניתן להדגמה
- **12 מסמכים רפואיים**, לפחות אחד מסוג MRI, אחד מכתב שחרור ואחד בדיקות דם
- **בלוקים** לשבועיים קדימה, בשלושת המצבים: מלא, חלקי, פנוי
- **4 קבצי PDF דמה** ב-`public/mock-files/`

---

## 11. נגישות

- ניגודיות טקסט מול רקע מינימום 4.5:1. `muted` על `canvas` נבדק במפורש
- כל יעד מגע לפחות 44×44px
- `focus-visible` נראה בכל רכיב לחיץ, לעולם לא `outline: none` ללא חלופה
- כל שדה מקושר ל-label דרך `htmlFor` / `id`
- מודל: מלכודת focus, סגירה ב-Escape, החזרת focus למקור בסגירה
- טבלה: `<caption>` מוסתר חזותית עם תיאור התוכן
- אייקון ללא טקסט מקבל `aria-label`
- שגיאות טופס מקושרות דרך `aria-describedby` ו-`aria-invalid`
- ניווט מלא במקלדת בכל תרחיש ליבה

---

## 12. סדר בנייה

| שלב | תוכן | תוצאה |
|---|---|---|
| 1 | פרויקט, Tailwind, tokens, RTL, גופן, `he.ts`, types, mock | `npm run dev` עולה עם עמוד ריק מסוגנן |
| 2 | primitives + data + overlay + `/kitchen-sink` | כל הרכיבים נראים בכל המצבים |
| 3 | AppShell, TopBar, DoctorSidebar, PatientNav, Router | ניווט בין כל ה-routes עובד |
| 4 | 7.1, 7.2, 7.7 - מסכי כניסה ואימות | זרימת כניסה מלאה לשני הקהלים |
| 5 | `MonthCalendar`, `BlockLegend` | לוח שנה עובד |
| 6 | **7.3 יומן ניתוחים** | המסך המרכזי, מלא ומלוטש |
| 7 | 7.4 כל הניתוחים | טבלה, סינון, ייצוא |
| 8 | 7.5 מודל החלפה | |
| 9 | 7.6 אשף בן 3 שלבים | כולל ולידציה מלאה |
| 10 | 7.8 מסך ראשי למטופל | |
| 11 | 7.9, 7.10 תורים עתידיים וקודמים | כולל סינון לפי מחלקה ורופא |
| 12 | 7.11, 7.12 פרטי תור ומסמכים | כולל הורדה |
| 13 | רספונסיביות, מצבי ריק, ליטוש | |

**נקודות עצירה להצגה ללקוח:** אחרי שלב 6, אחרי שלב 9, אחרי שלב 12.

---

## 13. Definition of Done

הפרוטוטייפ מוכן להצגה כאשר:

- [ ] כל 12 המסכים מסעיף 7 קיימים ונגישים דרך URL
- [ ] אפשר לעבור את כל חמשת תרחישי המנתח מקצה לקצה, בלחיצות בלבד
- [ ] אפשר לעבור את כל חמשת תרחישי המטופל מקצה לקצה
- [ ] הסינון לפי מחלקה ולפי רופא עובד בפועל על נתוני הדמה, ומשנה את הרשימה
- [ ] הורדת מסמך MRI פותחת קובץ אמיתי
- [ ] כל מסך נבדק ב-1440px, 1024px, 768px ו-390px
- [ ] כל מסך מציג מצב ריק תקין כשמרוקנים לו את הנתונים
- [ ] אין שגיאות או אזהרות בקונסול
- [ ] `/kitchen-sink` מציג את כל הרכיבים בכל המצבים
- [ ] אפשר לעבור את מסך היומן ואת מסך המטופל במקלדת בלבד

---

## 14. מחוץ לגבולות הפרוטוטייפ

לא נבנה בשלב זה, ויש לציין זאת בהצגה ללקוח:

- אימות אמיתי, ניהול הרשאות, session
- חיבור ל-API או לכל מקור נתונים חי
- העלאת קבצים אמיתית לשרת
- ייצוא אקסל אמיתי (במוקאפ - CSV מקומי)
- שליחת SMS
- אינטגרציה למערכות חיצוניות
- תמיכה בשפות נוספות
- בדיקות אוטומטיות

---

## 15. נספח - החלטות עיצוב שכדאי להציף ללקוח

רשימה קצרה של השינויים שאינם קוסמטיים בלבד, ושכדאי לדבר עליהם בפגישת ההצגה:

1. **הסרת הרקע המטושטש.** משפרת קריאות באופן דרמטי, אך משנה את תחושת המערכת.
2. **חיפוש בסרגל המנתחים.** הרשימה ארוכה מכדי לגלול בה.
3. **שורת סיכום יומית** ביומן ("4 ניתוחים, 4:20 שעות").
4. **מצב "פנוי" הפך לפעולה.** לחיצה על חלון פנוי פותחת יצירת ניתוח עם התאריך והשעה ממולאים.
5. **בחירת שעה במודל ההחלפה** הפכה מרשימה נפתחת לרשת חלונות פנויים.
6. **מקרא הבלוקים** אורגן מחדש, שורה לכל בית חולים.
7. **מצבים ריקים בעברית** במקום `No results found`.
8. **סינון המטופל בצ'יפים** ולא ב-Select, מכיוון שרוב השימוש יהיה מהנייד.
